import { ThemeOptions } from '@mui/material/styles';

const breakpoints: Partial<ThemeOptions['breakpoints']> = {
  values: {
    xs: 0,
    sm: 600,
    md: 900,
    lg: 1200,
    xl: 1488,
  },
};

export default breakpoints;
